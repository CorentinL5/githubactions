name: Exo3

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run script
        run: echo "Workflow exécuté toutes les 5 minutes ou manuellement"
